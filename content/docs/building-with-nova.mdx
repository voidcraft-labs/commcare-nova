---
title: Building an app with Nova
description: How Nova turns a conversation into a reviewed, working app and how to resume unfinished work.
---

## From a description to an app

When you describe a new app in chat, Nova first works out the people, records,
tasks, rules, and screens the workflow needs. It checks that design before it
starts building. Nova may ask questions when an answer changes the workflow or
when it needs information that is not safe to assume.

No placeholder app appears while that design is still being worked out. The
first app you see contains a coherent, valid workflow that you can open in the
builder and Preview. Nova can then add the remaining parts as valid updates;
unfinished private work never appears in the app.

## Resume an unfinished design

An app design that has not produced its first workflow appears under **Designs
in progress** on the app list. Choose **Resume** to return to the same
conversation and continue from its saved stage. Choose **Discard** when you no
longer want Nova to take that design further.

The design also has its own recovery address as soon as the first chat turn
starts. Refreshing the page returns to that design. If the app was created while
an update was lost in transit, Nova opens the authoritative app instead of
starting a second copy.

## If a later step stops

The app remains available when its first workflow was saved and a later part of
the build stops. Open its app card to review the conversation and continue the
build. A failed later step does not invalidate or hide the working revision that
already exists.

## Start with a blank app

Choose **Start with a blank app** when you want to begin directly in the
builder. That path creates Nova's smallest valid Survey, Form, and Question
starter immediately, without the reviewed conversational design flow.

## External setup

Some workflows depend on work outside the app, such as loading Project data or
configuring an automation in CommCare HQ. Nova names those prerequisites and
does not save a dependent part of the app until the required setup has durable
completion evidence. Preview does not pretend to run external automations.
