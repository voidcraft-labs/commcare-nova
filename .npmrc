# npm 12 denies Git and remote-tarball dependencies by default. Nova has one
# intentional root-owned remote tarball (SheetJS); keep transitive URLs denied.
allow-git=none
allow-remote=root

# Every current dependency install script is explicitly denied in package.json.
# Fail closed when a future dependency adds an unreviewed install script.
strict-allow-scripts=true
