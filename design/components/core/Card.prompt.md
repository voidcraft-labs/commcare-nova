Surface panel with a violet-tinted hairline border. Depth = surface/border contrast, not shadow.

```jsx
<Card padding={20}>
  <h3>Measles Investigation</h3>
  <p>2 modules · 5 forms</p>
</Card>

<Card interactive>…clickable app row…</Card>
```

Set `interactive` for hover-highlight + pointer (list rows, selectable cards). Default padding 16px.
