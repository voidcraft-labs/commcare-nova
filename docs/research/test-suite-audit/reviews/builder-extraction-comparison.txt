Display copy: tabs expanded to spaces; source comparison results are unchanged.

components/builder/app-setup/automationEditorModel.ts
EXACT cloneEditableValue
EXACT timedEventComparator
EXACT localIsoDate
EXACT weekdayIndexForIsoDate
EXACT remapWeeklyEventOffsets
EXACT automationTimeText
EXACT uuid
EXACT newCaseUpdate
EXACT newAlert
EXACT changeNewAutomationKind
EXACT pathStartsWith
EXACT automationCommitErrorPath
EXACT parseReminderIntervalDraft
EXACT automationLocationOptions
EXACT automationMessageReferenceCaseType
EXACT updateAutomationContextReferenceCaseTypes
EXACT recipientFor
EXACT recipientKindAvailable
EXACT clearLocationSettingsWithoutRecipient
EXACT contentFor
components/builder/shared/termEditorModel.ts
EXACT termMode
EXACT termsMatch
EXACT literalHasMeaningfulContent
EXACT termHasMeaningfulContent
EXACT termModeLabel
EXACT describeTermModeReplacement
EXACT constraintAdmitsType
EXACT propertyFilterFor
EXACT computeModeAdmission
EXACT buildTermAdmissionProbe
EXACT buildTermDefault
EXACT classifyLiteralShape
EXACT literalsMatch
EXACT buildLiteralForShape
EXACT describeLiteralShapeReplacement
components/builder/detail/appSettings/connectDraft.ts
EXACT configToDraft
CHANGED parseTimeEstimate
--- /tmp/nova-review-old-function   2026-09-06 20:35:32
+++ /tmp/nova-review-new-function   2026-09-06 20:35:32
@@ -1,4 +1,4 @@
 function parseTimeEstimate(raw: string): number | null {
    const n = Number(raw.trim());
-   return Number.isInteger(n) && n >= 1 ? n : null;
+   return Number.isSafeInteger(n) && n >= 1 ? n : null;
 }
\ No newline at end of file
EXACT draftParticipates
EXACT draftSectionsComplete
EXACT draftIdsValid
EXACT idBaseName
EXACT connectIdHelpers
EXACT assignDraftConnectIds
EXACT xpathOverride
EXACT draftToConfig
NEW hasDraftConnectParticipant
components/builder/PresenceRosterView.tsx
EXACT whereLabel
NEW PresenceRosterView
EXACT CompactRosterChip
EXACT AvatarFace
EXACT PeerHoverCard
EXACT PeerAvatar
EXACT OverflowChip
components/builder/shared/relationPathModel.ts
EXACT isRelationKind
EXACT kindLabel
EXACT caseTypeLabel
EXACT directChildCaseTypes
EXACT anyRelatedCaseTypes
EXACT availableCaseTypesForSingleRelation
EXACT declaredParentCaseType
EXACT relationKindIsAvailable
CHANGED relationKindHasAutomaticPath
--- /tmp/nova-review-old-function   2026-09-06 20:35:32
+++ /tmp/nova-review-new-function   2026-09-06 20:35:32
@@ -2,7 +2,15 @@
    kind: Exclude<RelationKind, "self">,
    originCaseType: string,
    caseTypes: readonly RelationCaseType[],
+   current?: RelationPath,
 ): boolean {
+   if (
+       (kind === "subcase" || kind === "any-relation") &&
+       (current?.kind === "subcase" || current?.kind === "any-relation") &&
+       current.identifier !== DEFAULT_IDENTIFIER &&
+       caseTypes.some((candidate) => candidate.name === current.ofCaseType)
+   )
+       return true;
    switch (kind) {
        case "ancestor":
            return declaredParentCaseType(originCaseType, caseTypes) !== undefined;
EXACT customRelationPath
EXACT unavailableKindDescription
CHANGED changeKind
--- /tmp/nova-review-old-function   2026-09-06 20:35:32
+++ /tmp/nova-review-new-function   2026-09-06 20:35:32
@@ -17,8 +17,18 @@
    const identifier = XML_ELEMENT_NAME_PATTERN.test(savedIdentifier)
        ? savedIdentifier
        : DEFAULT_IDENTIFIER;
-   const children = directChildCaseTypes(originCaseType, caseTypes);
-   const anyRelated = anyRelatedCaseTypes(originCaseType, caseTypes);
+   const children = availableCaseTypesForSingleRelation(
+       "subcase",
+       identifier,
+       originCaseType,
+       caseTypes,
+   );
+   const anyRelated = availableCaseTypesForSingleRelation(
+       "any-relation",
+       identifier,
+       originCaseType,
+       caseTypes,
+   );
    const savedChildType =
        value.kind === "subcase" || value.kind === "any-relation"
            ? value.ofCaseType
CHANGED relationChangeLosesStructure
--- /tmp/nova-review-old-function   2026-09-06 20:35:32
+++ /tmp/nova-review-new-function   2026-09-06 20:35:32
@@ -10,12 +10,20 @@
        return false;
    }
    if (value.kind === "any-relation" && nextKind === "subcase") {
-       const related = anyRelatedCaseTypes(originCaseType, caseTypes);
+       const related = availableCaseTypesForSingleRelation(
+           "any-relation",
+           value.identifier,
+           originCaseType,
+           caseTypes,
+       );
        const selected =
            value.ofCaseType ?? (related.length === 1 ? related[0]?.name : undefined);
-       return !directChildCaseTypes(originCaseType, caseTypes).some(
-           (candidate) => candidate.name === selected,
-       );
+       return !availableCaseTypesForSingleRelation(
+           "subcase",
+           value.identifier,
+           originCaseType,
+           caseTypes,
+       ).some((candidate) => candidate.name === selected);
    }
    if (value.kind === "ancestor") {
        return value.via.length > 1 || value.via[0].throughCaseType !== undefined;
EXACT relationReplacementCopy
EXACT withAncestorStep
EXACT withoutAncestorStep
EXACT ancestorRemovalConsequence
EXACT ancestorStepContexts
EXACT rebuildValidAncestorPath
