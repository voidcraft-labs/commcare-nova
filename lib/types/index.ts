/* Type re-exports from this barrel file are intentionally empty.
 * Settings types live in ./settings.ts and are imported directly.
 * App types are defined in lib/db/types.ts (Firestore schemas). */
